import 'package:flutter/material.dart';
import 'package:get/get.dart';

import 'ad_controller.dart';

class AdPage extends GetView<AdController> {
  const AdPage({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    // final Container adContainer = Container(
    //   alignment: Alignment.center,
    //   child: controller.adWidget,
    //   width: controller.myBanner.size.width.toDouble(),
    //   height: controller.myBanner.size.height.toDouble(),
    // );

    return Container(
      
    );
  }
}
