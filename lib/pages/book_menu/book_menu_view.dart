import 'package:flutter/material.dart';
import 'package:get/get.dart';
import 'book_menu_controller.dart';

class BookMenuPage extends GetView<BookMenuController> {
    const BookMenuPage({Key? key}) : super(key: key);

    @override
    Widget build(BuildContext context) {
    return Scaffold(
        body: Container(),
    );
    }
}
